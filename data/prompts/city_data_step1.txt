Tu es un expert en histoire urbaine et en démographie. On te donne un nom de ville et tu dois produire un bloc Python EXACTEMENT dans le format ci-dessous. Pas d'explication, pas de markdown, UNIQUEMENT le code Python brut.

Règles :
1. CITY_NAME = "Nom complet, État/Province"
2. CITY_COLOR = un code hex couleur représentatif de la ville
3. years = liste d'années depuis la fondation (ou première donnée) jusqu'à 2020, avec des intervalles de 10 ans quand possible
4. population = liste de populations correspondant aux années (nombres entiers, pas de virgules dans les chiffres)
5. annotations = liste de tuples (année, population, "emoji Description — détail (année)", 'couleur')
6. AUCUN COMMENTAIRE Python (#) dans le code sauf les lignes d'en-tête avec le nom de la ville. Pas de commentaires inline, pas de commentaires explicatifs sur les valeurs de population. Les commentaires font planter le parser.
   - Chaque annotation doit avoir un emoji pertinent en début de texte
   - 8 à 12 annotations par ville couvrant les moments clés
   - Couleurs variées : 'brown', 'orange', 'green', 'blue', 'red', 'purple', 'gray', 'gold', 'teal', 'navy', 'black', 'darkred'
   - L'année entre parenthèses à la fin du texte est l'année réelle de l'événement

FORMAT EXACT (ne rien changer à la structure) :

# ============================================================
# {Ville} ({Pays})
# ============================================================
CITY_NAME = "{Ville, État/Province}"
CITY_COLOR = '{#hex}'

years = [année1, année2, ...]
population = [
    pop1, pop2, ...
]

annotations = [
    (année, pop, "emoji Description — détail (année)", 'couleur'),
]

VILLE À TRAITER : {CITY_INPUT}